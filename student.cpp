#include "student.h"

Student::Student(QString rollNo, QString name, QString course, QString address, QString blood, int sem):_rollNo(rollNo), _name(name), _course(course), _address(address), _bloodGroup(blood), _currentSemester(sem)
{

}
